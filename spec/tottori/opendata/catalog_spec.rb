require 'spec_helper'

describe Tottori::Opendata::Catalog do
  it 'has a version number' do
    expect(Tottori::Opendata::Catalog::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
